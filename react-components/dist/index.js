import App from './App';
import ReactDOM from 'react-dom';
import React from 'react';
var domContainer = document.querySelector('#react_component');
ReactDOM.render(React.createElement(App), domContainer);
